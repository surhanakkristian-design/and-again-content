# pipeline run outputs (logs, intermediate JSON) — archived on Drive only
runs/

# python
__pycache__/
*.pyc

# macOS
.DS_Store

# local env files
.env
.env*.local

# Tinder music binaries live on Drive (AndAgain_music), not in git
tinder_music/*.mp3
