# Phase 2L - final Czech upload

`upload_cz_final.xlsx` (sheet `cz`) + `annotations_cz_final.jsonl`, 4,064 rows, integer `exercise_id`, `v[0] == en` on every row. **`v` is display-only and does not grade**: the Czech checker (stack_2l_cz) is SOURCE-ONLY. Checks: `UPLOAD_CZ_CHECK.md`.
