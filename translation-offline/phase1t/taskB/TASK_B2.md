# Phase 1T / Task B2 — Czech gold validation of the deterministic guards (0 model calls, PROBE not measurement)
Work dir ~/Projects/and-again-content/translation-offline. Write ONLY under phase1t/taskB/. Never modify a guard or any earlier phase dir. Budget ≈ 14 tool calls.
Read phase1t/taskB/INVENTORY_1T.md first (guard entry points, gold recipes, the Slovak-specific spots). Data: cz_sample_numbered.json (n, cz, sk, en, level), and the blind gold files cz_gold.json / sk_gold.json (schema in GOLD_SPEC_CZ.md; they are being written right now by blind annotators — build and dry-run your harness first, then wait/poll for the files with a short python loop of at most 8 minutes; if one is still missing, report what you have).
For each of the 120 sentence pairs run every deterministic guard's SOURCE-SIDE reader UNCHANGED, once on the Czech sentence and once on its Slovak sibling (the Slovak run is the control: it separates "Czech breaks it" from "real DB sentences, which are not arm-B, break it"). Annotation-driven inputs are empty for both (ann = {}), as they would be in production today — say so wherever a guard abstains for that reason.
 1. TIME FRAME: phase1n/f9.py `sk_frame` (and any other source-side frame reader the stack uses) vs gold tf. AGREE = same frame; CONSERVATIVE = guard abstains/None where gold has a frame (or gold "none"); ERROR = guard asserts a frame that differs from gold. Use the same definitions as phase1n validate_f9.py:36-44 and say if mine deviate.
 2. PERSON/NUMBER: F4v2 source-side reader (phase1i/checker_1i.py ~536-608) vs gold person. Same three classes.
 3. NOMINATIVE AGENT: AG's source-side agent reader — use BOTH phase1s/taskC/agent_drop_v2.py and, if it exists by then, phase1t/taskA/agent_drop_v3.py — vs gold subject / agent_nom / embedded_agents. ERROR = asserts an agent where gold has none, or a different agent; CONSERVATIVE = finds none where gold has one.
 4. PASSIVE / REFLEXIVE-PASSIVE / SUBJECTLESS detection (AG's abstain path: SK_REFLEX regex and the voice logic) vs gold voice. ERROR = treats a gold passive/reflexive_passive/impersonal sentence as an active-agent sentence (AG would then be free to reject a correct passive answer) or the reverse.
Additionally, for guard 3+4 together, the END-TO-END consequence: feed AG the English reference `en` as if it were the learner's answer (it is a correct answer by construction) and count how many references AG would REJECT on cz and on sk — each is a false rejection caused by the source-side reader; list them all.
Report per guard × language: agree / conservative / ERROR counts (n = 120, plus excluding gold fragments), and LIST EVERY ERROR (n, sentence, gold, guard output, the code spot responsible: file:line and the Slovak-specific list/regex/suffix involved). Then NAME EXPLICITLY every place where a guard reads Slovak-specific material and what it does on Czech (abstain / misfire / crash), confirming or correcting the three misfires predicted in INVENTORY_1T.md (f9.py:85-86 -l nouns; checker_1i.py:555-573 instrumental -em; agent_drop reflexive regex missing `se`) with the actual sentences that trigger them, or stating that none of the 120 does.
Output: CZ_GOLD_VALIDATION_1T.md (≤ 160 lines) + cz_validation.json (phase1s/safe_json.safe_dump). Final message ≤ 15 lines + tool-call count.
