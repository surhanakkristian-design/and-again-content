# Phase 1T — tooling task 1: assembler, judge packets, floor check (0 model calls)
Work dir ~/Projects/and-again-content/translation-offline. Write ONLY under phase1t/set/. Budget ≈ 12 tool calls. Use phase1s/safe_json.safe_dump for every JSON write. System python3, no third-party packages, PYTHONDONTWRITEBYTECODE=1.
Read: phase1t/set/WRITER_SPEC_1T.md, SPLIT_1T.md, JUDGE_BRIEF_1T.md; phase1p/assemble_1p.py, phase1p/floor_check_1p.py, phase1p/loader_1p.py (schemas to stay compatible with), phase1s/taskC/build_packet.py. Do NOT print or summarise the contents of phase1t/set/data/writer_*.json in your messages (the orchestrator stays blind to the items); you may run scripts over them.

Build
1. assemble_1t.py — adapted from assemble_1p.py. Input: data/writer_W{1,2,3,4}_part{1,2,3}.json (SIDs 1T001–1T100, numeric sid 180000+n, levels per SPLIT_1T.md). Output data/sentences.json, data/annotations.json, data/items.json in the loader_1p schema (item id "<C|W>:<sid>:<aid>"; keep the writer's sentence `kind`, `emb`, `tags` inside sentences[].tags.writer_tags and the answer tags in items[].tags; the known-tag list is the one in WRITER_SPEC_1T.md). REFUSE (exit 2, reasons listed) on: not 4 C + 5 W, duplicate answer strings inside a sentence, unknown tags, missing annotation fields, duplicate Slovak inside the set, and OVERLAP with any earlier sentence: exact-normalised match OR token-Jaccard ≥ 0.75 against every Slovak sentence of every earlier phase (phase1n/existing_350.json, the 1N set, phase1p/data/sentences.json, and any other phase*/data/sentences*.json or phase*/fresh/*.json you can find — list the sources and the total count of distinct earlier sentences; it should be about 570). With `--report` instead of refusing, write ASSEMBLE_REPORT_1T.md listing every violation by sid so single sentences can be sent back to the writer.
   `--labels` joins the judge verdicts (judge/verdicts_P*.json via judge/_key.json) into data/labels.json: per item judged, type, passive, agent_drop, tip, borderline; plus judge noise on the 80 duplicate controls (label disagreements, type-only disagreements).
2. build_packets_1t.py — 900 items + 80 hidden duplicate controls (random items repeated under a second jid, stratified 20 per level), shuffled ACROSS levels with seed 20260919, split into 4 packets judge/packet_P1..P4.json of 245 items, each item ONLY {jid, slovak, answer, level}; judge/_key.json (jid → item id, duplicate-of) is written OUTSIDE the judge's reading list (keep it in judge/_private/). 
3. floor_check_1t.py — on JUDGED labels, before any run: (F1) items tagged agentdrop-main|agentdrop-embedded AND judged wrong ≥ 120; (F2) tagged agentdrop-embedded AND judged wrong ≥ 60; (F3) tagged timeframe AND judged wrong ≥ 100; (F4) tagged by-passive|by-passive-embedded AND judged correct ≥ 60; (F5) judged-correct answers of SKP sentences (Slovak passive / reflexive-passive / subjectless) ≥ 40; plus a T/W/M/S table of judged-wrong types and writer-intent × judged cross-table, per level and per half. Exit 2 + STOP_FLOOR.txt unless all five hold; write FLOOR_CHECK_1T.md/.json either way. `--make-fixture` builds a synthetic writer+verdict fixture under selftest/ so everything can be self-tested before real data is complete.
Self-test all three on the fixture (including a fixture that violates each refusal and each floor) and report pass/fail counts. If the real writer files are already complete when you finish, run `assemble_1t.py --report` and `build_packets_1t.py` on them and report ONLY counts (sentences, items, violations by kind, overlap hits), never sentence or answer text.
Final message ≤ 12 lines + your tool-call count.
