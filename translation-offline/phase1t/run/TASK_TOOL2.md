# Phase 1T — tooling task 2: the runner for the fresh set (build + 0-call checks ONLY; you make NO Gemini call and you NEVER start --final)
Work dir ~/Projects/and-again-content/translation-offline. Write ONLY under phase1t/run/. Never modify earlier phase dirs or phase1t/set/data. Budget ≈ 15 tool calls. python3 system, PYTHONDONTWRITEBYTECODE=1.
You are the RUNNER side: you must never read judge labels (phase1t/set/data/labels.json, phase1t/set/judge/verdicts_*.json, judge/_private/) and must not print item texts in your messages.

Read: phase1p/HANDOFF_RUNNER_1P.md, phase1p/runner_1p.py, phase1p/loader_1p.py, phase1p/FROZEN_CONFIG_1P.json, phase1p/FREEZE_FILES, phase1s/taskD/RUNNER_PATCH_1S.md, phase1s/safe_json.py, phase1s/taskC/ag_apply.py + rescore_1s_c.py (how K3 = "lever 1 off + AG before L3" was scored), phase1t/taskA/agent_drop_v3.py + AG_V3_REGRESSION.md, phase1t/set/SPLIT_1T.md, phase1t/set/assemble_1t.py (data schema; data in phase1t/set/data/{sentences,annotations,items}.json once assembled — may not exist yet: use phase1t/set/selftest fixtures until it does).

Build phase1t/run/runner_1t.py (+ loader_1t.py) = the 1Q/1P frozen stack UNCHANGED (same prompt, model gemini-3.1-flash-lite, temperature 0, thinkingBudget 0, LOCKTIP, F2B, F3, F4v2, F5, levers 2 and 3, tip_reject=True) with exactly these differences:
 1. lever 1 REMOVED (no rewritten side, no extra calls).
 2. rule AG v3 (phase1t/taskA/agent_drop_v3.py, all flags on) placed with the deterministic guards BEFORE L3: an AG reject is final and costs 0 calls (the item never reaches L3). Record per item also the 0-call SHADOW verdicts of AG v2 and of each v3 flag alone, for attribution; and because AG-rejected items never reach L3, nothing else may depend on them.
 3. RUNNER_PATCH_1S applied in full: safe_json.safe_dump at every write site, serialise-before-truncate, guarded_run so that DONE or CRASHED(+traceback) always exists, FINAL_RUN_DONE written in a finally-safe way only after results are safely on disk; every model reply appended to calls.jsonl BEFORE any aggregation so a crash can be replayed offline with 0 calls.
 4. Data = phase1t/set/data; halves by SPLIT_1T.md (odd sid → P1); numeric sid 180000+n.
 5. CALL CAP: read from FROZEN_CONFIG_1T.json key "call_cap"; if the key is null/missing the runner REFUSES --final (the owner has not fixed the cap yet). --preflight prints planned calls = number of items that reach L3 after all deterministic layers (count it exactly by a 0-call pass), asserts L2 firings > 0, stops on L3-eligible 0, and writes STOP_PLAN.txt if planned > cap − 10.
 6. --final: refuses if FINAL_RUN_DONE exists; refuses unless every file in FREEZE_FILES matches FREEZE_HASH (git commit); refuses unless phase1t/set/FLOOR_CHECK_1T.json says all five floors hold; reads labels ONLY after every planned verdict exists; a quota wall writes RUN_PAUSED.txt and exits 3 before any label is read (resume continues from calls.jsonl without repeating calls). access_log.jsonl: one line per data/label read, 1N convention.
 7. Scoring (score_1t.py, separate file, runs after the run with 0 calls from rows): pooled + P1 + P2 coverage and FA with exact Clopper-Pearson 95 % intervals (self-test: 8/481 → [0.72, 3.25]; 348/385 → [87.00, 93.14]), Fisher exact P1 vs P2 for both, per level, FA by judged type T/W/M/S, the cells: agent-drop (all / main / embedded, by writer tag ∧ judged wrong), by-passive coverage (must be reported as k/n, expectation 100 %), SKP-sentence coverage (AG abstain path), time-frame FA; false rejections and false accepts by layer; AG v3 catches and MEASURED COST (judged-correct items AG rejects, listed); AG v2-shadow comparison; and EVERY figure twice: primary labels and S2 (every item the judge flagged borderline=true with a non-empty "dropped" field and judged wrong is scored as correct). Judge noise from the 80 duplicate controls.
 8. --dry-run on synthetic rows / the selftest fixture with a stubbed model (0 calls) exercising: AG reject path, L3 path, TIP reject, quota pause + resume, a crash inside aggregation (DONE/CRASHED marker + offline replay gives identical results), exotic JSON keys/values.
Write FROZEN_CONFIG_1T.json (call_cap: null), FREEZE_FILES (every .py the run imports, including phase1t/taskA/agent_drop_v3.py and the imported earlier-phase modules), HANDOFF_RUNNER_1T.md (commands in order, ≤ 60 lines), CALL_PLAN_1T.md (planned calls once real data exists; else say pending).
Run the dry-run and, if phase1t/set/data/items.json exists, the --preflight (0 calls). Report: dry-run checks passed/failed, planned calls, anything in the 1P runner you had to change beyond the list above (there should be nothing — list every diff hunk category in TOOLING_EDITS_1T.md). Final message ≤ 15 lines + tool-call count.
