# Task: plausible WRONG translations (Sonnet). Open ONLY measure/fa_writer_input.json. Two tool calls: read, write.
Each item has the native sentences, the correct English `en` and the practised span `en_answer`. Write 3 plausible WRONG
translations a learner might type, each wrong for ONE clear reason, rotating these types: T = wrong tense / practised
structure (e.g. "will" after "if", Past Simple instead of Past Perfect); W = a content word with a different meaning
(fork/spoon, left/right, buy/sell); M = meaning missing or added (dropped element, added negation, extra detail);
S = wrong subject/person where the native sentence fixes it. Each must be really wrong, never an acceptable alternative.
Write measure/fa_translations.json: [{"exercise_id":…,"items":[{"text":…,"type":"T|W|M|S","why":"≤10 words"}]}].
