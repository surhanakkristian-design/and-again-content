# TRANSLATION-ONLY stack: every change against the frozen 1W originals

Generated by `phase2i/make_tonly.py`; the full unified diff is `phase2i/tonly.diff`. Line numbers are those of the ORIGINAL file. The originals are imported nowhere by the TRANSLATION-ONLY stack: `stack_tonly.py` loads the three copies under their original module names before anything else, every other module of the 1W chain is imported in place, unchanged. Also: `stack_tonly.py` and `run_2i.py` strip `lk` and every `lk_*` key from the annotation (top level, `hygienised`, `raw`) before the stack sees it.

## 1. phase1i/lib_prev.py:127-128

Why: L2 structure lock removed: route() no longer vetoes on lock_ok / reads locks

Removed / replaced text:
```
    if not it['lock_ok']:
        return 'L2', 'practised grammar span not present (lock %s)' % ' | '.join(it['locks'][:3])
```
Replacement: (nothing)

## 2. phase1i/lib_prev.py:233-235

Why: the L3 prompt line naming the practised grammar and declaring it ALREADY VERIFIED removed

Removed / replaced text:
```
    if variant == 'P-B':
        lines.insert(3, 'Practised grammar: %s — ALREADY VERIFIED as correct in this answer; judge meaning and '
                        'vocabulary only.' % it['topic'])
```
Replacement: (nothing)

## 3. phase1i/lib_prev.py:17

Why: path pin only: the copy resolves every path exactly like the original (same data, config and sys.path dirs); no verdict effect

Removed / replaced text:
```
__file__
```
Replacement: 

```
'/Users/kristiansurhanak/Projects/and-again-content/translation-offline/phase1i/lib_prev.py'
```

## 4. phase1i/pipeline_1i.py:105-106

Why: the checker item no longer carries locks / lock_ok / lock_released_2_1

Removed / replaced text:
```
'wrong_why': '', 'locks': r['locks'],
            'lock_ok': r['lock_ok'], 'lock_released_2_1': r['lock_released_2_1'],
```
Replacement: 

```
'wrong_why': '',
```

## 5. phase1i/pipeline_1i.py:31

Why: F2B switched off (reads the lock span)

Removed / replaced text:
```
'F2B': 1}
```
Replacement: 

```
'F2B': 0}
```

## 6. phase1i/pipeline_1i.py:26

Why: path pin only: the copy resolves every path exactly like the original (same data, config and sys.path dirs); no verdict effect

Removed / replaced text:
```
__file__
```
Replacement: 

```
'/Users/kristiansurhanak/Projects/and-again-content/translation-offline/phase1i/pipeline_1i.py'
```

## 7. phase1p/runner_1p.py:150-152

Why: the one place that turns annotation lk into locks removed

Removed / replaced text:
```
        lk = []
        for x in (hy.get('lk') or []):
            lk += [x] if isinstance(x, str) else [y for y in x if isinstance(y, str)]
```
Replacement: (nothing)

## 8. phase1p/runner_1p.py:159

Why: record no longer carries locks

Removed / replaced text:
```
'half': half_of(sid), 'locks': lk,
```
Replacement: 

```
'half': half_of(sid),
```

## 9. phase1p/runner_1p.py:164

Why: record no longer carries lock_ok / lock_released_2_1

Removed / replaced text:
```
'rows': [], 'lock_ok': True, 'lock_released_2_1': None}
```
Replacement: 

```
'rows': []}
```

## 10. phase1p/runner_1p.py:165-173

Why: lock_ok / lock_equivalent_ok computation removed

Removed / replaced text:
```
        try:
            nrm = C.base.norm
            r['lock_ok'] = (not lk) or any(nrm(x).strip() in nrm(r['answer']) for x in lk)
            if not r['lock_ok']:
                ok, tr = C.lock_equivalent_ok(P.to_item(r))
                if ok:
                    r['lock_ok'], r['lock_released_2_1'] = True, tr
        except Exception:
            pass
```
Replacement: (nothing)

## 11. phase1p/runner_1p.py:223-224

Why: F8/F9 readouts dropped (F9 reads lk; neither decides: f8_on=f9_on=False)

Removed / replaced text:
```
    g = R1K.guard_readouts(recs, ann, False)
    guards = {i: {'f8': None, 'f9': g[i]['f9']} for i in g}     # F8 CANNOT decide, F9 does not
```
Replacement: 

```
    guards = {}
```

## 12. phase1p/runner_1p.py:229

Why: LOCKTIP off (locktip=False)

Removed / replaced text:
```
R1K.configure_row(stx, recs, vm, True, guards, False, False, True)
```
Replacement: 

```
R1K.configure_row(stx, recs, vm, False, guards, False, False, True)
```

## 13. phase1p/runner_1p.py:233-234

Why: L3 plan without LOCKTIP / lock counts

Removed / replaced text:
```
    lock_rej, l3 = RL.lock_counts(st, recs)
    return lock_rej, l3
```
Replacement: 

```
    return [], RL.l3_ids(st, False)
```

## 14. phase1p/runner_1p.py:18

Why: path pin only: the copy resolves every path exactly like the original (same data, config and sys.path dirs); no verdict effect

Removed / replaced text:
```
__file__
```
Replacement: 

```
'/Users/kristiansurhanak/Projects/and-again-content/translation-offline/phase1p/runner_1p.py'
```

## Why nothing else changed

- Only three modules are copied (lib_prev, pipeline_1i, runner_1p); `tonly.diff` shows the complete difference and contains no hunk outside the edits above plus the `__file__` path pins.
- Kept unchanged, imported in place: checker_1i (F3, F5, F4v2, L3 verdict map, TIP handling), runner_1k.configure_row / apply_guards, runner_1u (build_req_1u: GROUND, WORDING, VOICE_SAME, lever 2/3 lines, ARTICLE_LINE_1U; ag_map_1u; build_rows -> stack_1w.final_accept = AG v4 + v5 rs_nom + tip_det_rule), stack_1v, reader_nom, agent_drop_v4, lever2/3, SYS text, GEN_CFG, the model.
- `stack_tonly.py` sets the loaded lib_prev copy's `__file__` to phase1i/lib_prev.py after loading it, because checker_1i.py:32 asserts `lib_prev` lives in phase1i/ (a location check, no verdict effect); `__spec__.origin` keeps the copy path, which IMPORT_GRAPH.json reports.
- TIP-as-rejection stays ON: `configure_row(..., tip_reject=True)` (last argument unchanged).
- F1 and F2 flags are left True: they live only in the L2 lock branch of checker_1i.decide, which is unreachable once route() has no lock line (a mistake-pattern L2 returns before them). The poisoned-item test proves they are never reached (f1_lock_ok / f2_equivalent read it["locks"]).
- phase1i/taskB/lock_fix.py still patches C.f1_lock_ok / C.lock_equivalent_ok at state build; nothing calls them in this stack (no lock read, proven by the same test).
- The test suite (test_2i.py (f)) asserts the TONLY user prompt equals the FROZEN prompt minus exactly the one Practised-grammar line, with identical system text and generation config.
