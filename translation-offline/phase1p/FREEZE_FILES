# Phase 1P freeze list. check_freeze() refuses if any .py in phase1p is missing here,
# so the list is exhaustive; the first block is what actually executes at run time.
# --- executed at run time (--final) ---
runner_1p.py
loader_1p.py
lever1.py
lever2.py
lever3.py
# --- present in phase1p, NOT executed by --final ---
assemble_1p.py
floor_check_1p.py
normalise_1p.py
